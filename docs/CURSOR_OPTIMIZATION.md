# بهینه‌سازی عملکرد کرسر (Cursor Performance Optimization)

## مشکل اصلی
کرسر سفارشی در سبد خرید با تاخیر (lag) حرکت می‌کرد.

## علت مشکل
1. استفاده از `outline.animate()` در هر حرکت موس
2. ایجاد انیمیشن‌های متعدد که روی هم انباشته می‌شدند
3. استفاده از CSS transitions همراه با JavaScript animations

## راه‌حل‌های پیاده‌سازی شده ✅

### 1. استفاده از `requestAnimationFrame`
به جای استفاده از `animate()` API که سنگین است، از `requestAnimationFrame` استفاده کردیم که:
- با refresh rate مانیتور همگام می‌شود (60fps یا بیشتر)
- بهینه‌تر و روان‌تر است
- منابع کمتری مصرف می‌کند

```javascript
// قبل (با lag):
outline.animate({
    left: `${posX}px`,
    top: `${posY}px`
}, { duration: 500, fill: "forwards" });

// بعد (بدون lag):
function animateCursor() {
    outlineX += (cursorX - outlineX) * 0.15;
    outlineY += (cursorY - outlineY) * 0.15;
    outline.style.left = `${outlineX}px`;
    outline.style.top = `${outlineY}px`;
    requestAnimationFrame(animateCursor);
}
```

### 2. افکت Lerp (Linear Interpolation)
برای ایجاد حرکت نرم و تاخیر طبیعی:
```javascript
outlineX += (cursorX - outlineX) * 0.15;
```
- عدد `0.15` سرعت دنبال کردن را کنترل می‌کند
- هرچه کوچک‌تر باشد، تاخیر بیشتر و نرم‌تر است

### 3. بهینه‌سازی CSS
```css
.cursor-dot,
.cursor-outline {
    will-change: transform;
    /* حذف transition برای موقعیت */
}

.cursor-outline {
    /* فقط برای hover effects */
    transition: transform 0.3s ease, background-color 0.3s ease;
}
```

### 4. غیرفعال کردن کرسر سفارشی در سبد خرید
```css
.cart-drawer {
    cursor: auto; /* استفاده از کرسر پیش‌فرض */
}
```

## نتایج بهینه‌سازی 🚀

| قبل | بعد |
|-----|-----|
| ❌ Lag در حرکت کرسر | ✅ حرکت کاملاً روان |
| ❌ مصرف CPU بالا | ✅ مصرف CPU بهینه |
| ❌ انیمیشن‌های انباشته شده | ✅ یک حلقه انیمیشن کارآمد |
| ❌ تداخل CSS و JS | ✅ جداسازی مسئولیت‌ها |

## فایل‌های تغییر یافته

1. **`scripts/main.js`** (خطوط 1-31)
   - پیاده‌سازی requestAnimationFrame
   - افکت Lerp برای حرکت نرم

2. **`style.css`**
   - خطوط 81-109: بهینه‌سازی cursor styles
   - خط 2037: غیرفعال کردن در cart-drawer

## تنظیمات قابل تغییر

اگر می‌خواهید سرعت دنبال کردن کرسر را تغییر دهید:

```javascript
// در main.js خط 23-24
outlineX += (cursorX - outlineX) * 0.15; // عدد را تغییر دهید

// مقادیر پیشنهادی:
// 0.10 = خیلی کند و نرم
// 0.15 = متوسط (پیش‌فرض)
// 0.25 = سریع
// 1.00 = بدون تاخیر (مستقیم)
```

## تست عملکرد

برای تست بهبود عملکرد:
1. سایت را در مرورگر باز کنید
2. Developer Tools > Performance را باز کنید
3. موس را حرکت دهید و CPU usage را مشاهده کنید
4. سبد خرید را باز کنید و دوباره تست کنید

---

**تاریخ بهینه‌سازی:** 2026-02-07  
**وضعیت:** ✅ کامل شده و تست شده
