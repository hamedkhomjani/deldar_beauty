---
/**
 * Booking modal with calendar + time slots + details form.
 * Logic lives in scripts/booking.ts; service list comes from data/services.ts.
 */
import { bookingServiceNames } from '../data/services';
import { asset, SALON } from '../config';
import { t, type Lang } from '../i18n';

interface Props {
  lang?: Lang;
}

const { lang = 'fa' } = Astro.props;
const dict = t(lang);
const services = bookingServiceNames(lang);

const siteUrl = 'https%3A%2F%2Fhamedkhomjani.github.io%2Fdeldar_beauty%2F';
const shareText = encodeURIComponent(dict.shareText);
---

<div id="booking-modal" class="modal" role="dialog" aria-modal="true" aria-label={dict.bookingDialogAria}>
  <div class="modal-content container">
    <span class="close-modal" id="close-booking" aria-label={dict.bookingCloseAria}>&times;</span>

    <!-- Step 1: Calendar & Details -->
    <div id="booking-step-1" class="booking-step">
      <div class="modal-header">
        <img src={asset('assets/images/logo.svg')} alt={dict.logoAlt} class="modal-logo" />
        <h2>{dict.bookingModalHeading}</h2>
      </div>

      <div class="booking-grid">
        <div class="calendar-container">
          <h3>{dict.chooseDay}</h3>
          <div class="calendar-card">
            <div class="calendar-header">
              <span class="prev-month" aria-label={dict.prevMonthAria}>
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <polyline points="15 18 9 12 15 6"></polyline>
                </svg>
              </span>
              <span class="current-month"></span>
              <span class="next-month" aria-label={dict.nextMonthAria}>
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <polyline points="9 18 15 12 9 6"></polyline>
                </svg>
              </span>
            </div>
            <div class="calendar-body">
              <div class="days-header">
                {dict.weekdays.map((d) => <span>{d}</span>)}
              </div>
              <div class="days-grid" id="calendar-days"></div>
              <div class="quick-days">
                <button type="button" class="quick-day" data-offset="0">{dict.today}</button>
                <button type="button" class="quick-day" data-offset="1">{dict.tomorrow}</button>
              </div>
            </div>
          </div>
        </div>

        <div class="time-container">
          <h3>{dict.chooseTime}</h3>
          <div class="time-slots" id="time-slots">
            {dict.timeSlots.map((slot, i) => (
              <button class:list={['time-slot', { active: i === 1 }]}>{slot}</button>
            ))}
          </div>
          <div class="time-legend">
            <span><i class="dot reserved"></i> {dict.reservedLegend}</span>
            <span><i class="dot available"></i> {dict.availableLegend}</span>
          </div>
          <p id="booking-summary" class="booking-summary">{dict.summaryPlaceholder}</p>
        </div>
      </div>

      <div class="details-section">
        <h3>{dict.detailsHeading}</h3>
        <form id="booking-form" class="booking-form">
          <div class="form-group">
            <select id="booking-service" required>
              <option value="" disabled selected>{dict.servicePlaceholder}</option>
              {services.map((name) => <option>{name}</option>)}
              <option>{dict.freeConsultOption}</option>
            </select>
            <span class="required-star">*</span>
          </div>
          <div class="form-row">
            <div class="form-group">
              <input type="text" id="booking-name" placeholder={dict.namePlaceholder} required />
              <span class="required-star">*</span>
            </div>
            <div class="form-group">
              <input type="tel" id="booking-phone" placeholder={dict.phonePlaceholder} required />
              <span class="required-star">*</span>
            </div>
          </div>
          <div class="form-group">
            <input type="email" id="booking-email" placeholder={dict.emailPlaceholder} />
          </div>
          <p id="booking-form-msg" class="form-msg"></p>
          <button type="submit" class="btn-confirm">{dict.bookingSubmitBtn}</button>
        </form>
      </div>
    </div>

    <!-- Step 2: Confirmation -->
    <div id="booking-step-2" class="booking-step hidden">
      <div class="success-content">
        <div class="success-icon">
          <svg viewBox="0 0 24 24" width="80" height="80" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
        </div>
        <h2>{dict.bookingSuccessHeading}</h2>
        <p>
          {dict.bookingSuccessText}
        </p>
        <button class="btn-back" id="btn-return-home">{dict.bookingBackBtn}</button>

        <div class="social-share">
          <p>{dict.sharePrompt}</p>
          <div class="share-icons">
            <a
              href={`https://t.me/share/url?url=${siteUrl}&text=${shareText}`}
              target="_blank"
              rel="noopener"
            >
              <img src="https://cdn-icons-png.flaticon.com/512/2111/2111646.png" alt="Telegram" width="30" />
            </a>
            <a href={`https://instagram.com/${SALON.instagram}`} target="_blank" rel="noopener">
              <img src="https://cdn-icons-png.flaticon.com/512/174/174855.png" alt="Instagram" width="30" />
            </a>
            <a
              href={`https://wa.me/?text=${shareText}%20-%20${siteUrl}`}
              target="_blank"
              rel="noopener"
            >
              <img src="https://cdn-icons-png.flaticon.com/512/1358/1358023.png" alt="Share" width="30" />
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
